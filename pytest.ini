[pytest]
# Exclude network/integration tests by default (audit M5). Opt in with:
#   pytest -m integration
addopts = -m "not integration"
markers =
    integration: tests that may require external services or network access
