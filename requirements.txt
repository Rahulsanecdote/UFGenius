# Top-level dependencies for local development and CI.
# Install with constraints for deterministic resolution:
#   python -m pip install -r requirements.txt -c constraints.txt

yfinance>=0.2.54,<1.0
pandas
numpy<2
vaderSentiment
newsapi-python
praw
requests
python-dotenv
PyYAML
schedule
matplotlib
scikit-learn
fredapi
beautifulsoup4
alpaca-py
lxml
flask
gunicorn
pytest

# The optional P3.1 explainability layer (explain.enabled) needs the Anthropic
# SDK — kept OUT of the base install so a default-off feature doesn't ship an
# LLM SDK to every deployment. Operators who enable it install it separately:
#   pip install -r requirements-explain.txt
